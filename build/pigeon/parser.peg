{package query
// part of the initializer code block omitted for brevity
}

Input <- expr:Expr EOF {
    return expr, nil
}

Expr "Expression" <- _ .+ _ {
    return fmt.Sprintf(`%s`,c.text), nil
}

_ "whitespace" <- [ \n\t\r]*

EOF <- !.